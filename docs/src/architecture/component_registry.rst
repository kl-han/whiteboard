Component registry
==================

.. This page is generated by docs/tools/component_registry.py. Edit the
   data there and regenerate; do not edit this file by hand.

A census of every subsystem we found by scanning manifests, file types,
build scripts and runtime entrypoints, not only the pnpm workspace list.
Scan summary (first-party means outside ``apps/review-desktop/code-oss``):

* First-party: 599 ``.ts``, 133 ``.tsx``, 74 ``.mjs``, 20 ``.py``, 13 ``.sh``,
  and 2 ``.rs`` / 2 ``.go`` (LSP e2e fixtures only).
* Vendored Code - OSS: 6,406 files, including 4,940 ``.ts`` and 77 ``.rs``
  (upstream ``cli/`` and ``build/win32/``).
* **No first-party Rust, Cargo workspace or WASM build exists in this
  repository.** The Rust semantic-diff engine is the external ``diffr``
  binary. See :doc:`semantic_diff/index`.

Summary
-------

.. list-table::
   :header-rows: 1
   :widths: 26 18 22 18 16

   * - Component
     - Language
     - Runtime
     - Ownership
     - Confidence
   * - :ref:`Whiteboard CLI <reg-whiteboard-cli>`
     - TypeScript
     - Node 24, or Electron-as-Node when delegated to Desktop's bundled CLI
     - Whiteboard-owned
     - CONFIRMED
   * - :ref:`Embedded Desktop server <reg-embedded-desktop-server>`
     - TypeScript
     - Node inside an Electron ``UtilityProcess``
     - Whiteboard-owned
     - CONFIRMED
   * - :ref:`Headless server <reg-headless-server>`
     - TypeScript
     - Node 24 (foreground process)
     - Whiteboard-owned
     - CONFIRMED
   * - :ref:`JSON review store and HTTP API <reg-json-review-store-and-http-api>`
     - TypeScript
     - Node (inside either server)
     - Whiteboard-owned
     - CONFIRMED
   * - :ref:`MCP adapter <reg-mcp-adapter>`
     - TypeScript
     - Node, a stdio child process of the agent
     - Whiteboard-owned (uses the external ``@modelcontextprotocol/sdk``)
     - CONFIRMED
   * - :ref:```api`` adapter <reg-api-adapter>`
     - TypeScript
     - Node, one-shot
     - Whiteboard-owned
     - CONFIRMED
   * - :ref:`Canvas <reg-canvas>`
     - TypeScript/TSX (React 19)
     - Chromium renderer inside the Desktop workbench (a dynamically imported ES module)
     - Whiteboard-owned
     - CONFIRMED
   * - :ref:`libavoid WASM edge router <reg-libavoid-wasm-edge-router>`
     - C++ compiled to WebAssembly
     - WASM in the canvas's Chromium renderer
     - External (LGPL-2.1-or-later)
     - CONFIRMED
   * - :ref:`Whiteboard workbench (fork-owned code) <reg-whiteboard-workbench--fork-owned-code>`
     - TypeScript
     - Electron main, renderer and utility processes
     - Whiteboard-owned inside a vendored tree
     - CONFIRMED
   * - :ref:`Vendored Code - OSS <reg-vendored-code---oss>`
     - TypeScript (+ upstream Rust ``cli/`` and ``build/win32``, both unbuilt here)
     - Electron 42.10.0
     - Vendored upstream, with enumerated divergences
     - CONFIRMED
   * - :ref:`diffr semantic-diff engine <reg-diffr-semantic-diff-engine>`
     - Rust (derived from difftastic; tree-sitter 0.26, git2, wasmtime 34)
     - Native executable (macOS arm64, Linux x64 only)
     - External (dev.fast-owned, separate repository)
     - CONFIRMED (binary behavior); UNVERIFIED (Rust source internals)
   * - :ref:```@dev.fast/diffr`` contract package <reg-dev-fast-diffr-contract-package>`
     - TypeScript + Node ESM script
     - Node (fetcher); anywhere zod runs (schemas)
     - External (dev.fast)
     - CONFIRMED
   * - :ref:`review-protocol <reg-review-protocol>`
     - TypeScript (zod 4)
     - Node, browser, and generated into the fork
     - Whiteboard-owned (private)
     - CONFIRMED
   * - :ref:`Sharing + review-share-protocol <reg-sharing---review-share-protocol>`
     - TypeScript
     - Node
     - Whiteboard-owned
     - CONFIRMED (client); UNVERIFIED (service)
   * - :ref:`trace-core <reg-trace-core>`
     - TypeScript
     - Node (CLI and hooks), and inside the server
     - Whiteboard-owned (private)
     - CONFIRMED
   * - :ref:`trace-protocol <reg-trace-protocol>`
     - TypeScript (zod)
     - Node (CLI) and the external store server
     - Whiteboard-owned, published
     - CONFIRMED
   * - :ref:`local-vcs <reg-local-vcs>`
     - TypeScript
     - Node; shells out to ``git`` and ``jj``
     - Whiteboard-owned, published
     - CONFIRMED
   * - :ref:`json <reg-json>`
     - TypeScript
     - Node / browser
     - Whiteboard-owned (private)
     - CONFIRMED
   * - :ref:`Claude / Codex / Cursor plugins <reg-claude---codex---cursor-plugins>`
     - JSON manifests
     - The agent host
     - Whiteboard-owned
     - CONFIRMED
   * - :ref:`OpenCode plugin <reg-opencode-plugin>`
     - JavaScript
     - OpenCode's plugin runtime
     - Whiteboard-owned
     - CONFIRMED
   * - :ref:`Pi skill package <reg-pi-skill-package>`
     - Markdown skill
     - Pi agent
     - Whiteboard-owned
     - CONFIRMED
   * - :ref:`Agent instructions <reg-agent-instructions>`
     - Markdown
     - Served by the server
     - Whiteboard-owned
     - CONFIRMED
   * - :ref:`Legacy importer <reg-legacy-importer>`
     - TypeScript
     - Node (startup, ``migrate apply``)
     - Whiteboard-owned (legacy support)
     - CONFIRMED
   * - :ref:`Telemetry <reg-telemetry>`
     - TypeScript
     - Node (servers and CLI); events relayed from the renderer
     - Whiteboard-owned; the PostHog service is external
     - CONFIRMED
   * - :ref:`Curated extensions and language servers <reg-curated-extensions-and-language-servers>`
     - Various (third-party VSIX)
     - Code - OSS extension host
     - External
     - CONFIRMED
   * - :ref:`Desktop build and packaging scripts <reg-desktop-build-and-packaging-scripts>`
     - Bash, Node ESM, Python
     - Developer machine / CI runner
     - Whiteboard-owned
     - CONFIRMED
   * - :ref:`CLI release and smoke scripts <reg-cli-release-and-smoke-scripts>`
     - Node ESM
     - CI
     - Whiteboard-owned
     - CONFIRMED
   * - :ref:`GitHub Actions workflows <reg-github-actions-workflows>`
     - YAML
     - ``ubuntu-latest``, ``review_big_boy``, ``macos-15-xlarge``
     - Whiteboard-owned
     - CONFIRMED
   * - :ref:`anti-slop lint plugin <reg-anti-slop-lint-plugin>`
     - TypeScript (oxlint JS plugin)
     - oxlint
     - Vendored (partially) + Whiteboard-owned rules
     - CONFIRMED
   * - :ref:`review-latency harness <reg-review-latency-harness>`
     - Python ≥3.12
     - CPython via ``uv``
     - Whiteboard-owned (developer tool)
     - CONFIRMED
   * - :ref:`Sphinx documentation <reg-sphinx-documentation>`
     - reStructuredText + Python config
     - Python / Sphinx 9
     - Whiteboard-owned
     - CONFIRMED
   * - :ref:`progressive-review remnant <reg-progressive-review-remnant>`
     - TypeScript
     - —
     - Legacy
     - HISTORY
   * - :ref:`Upstream Rust in Code - OSS <reg-upstream-rust-in-code---oss>`
     - Rust
     - —
     - Vendored upstream
     - INFERRED

Entries
-------

.. _reg-whiteboard-cli:

Whiteboard CLI
~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``packages/review/src/cli.ts``, ``cli-runner.ts``
   * - Product feature
     - Launch Desktop, headless authoring, traces, sharing, instance selection
   * - Language
     - TypeScript
   * - Runtime
     - Node 24, or Electron-as-Node when delegated to Desktop's bundled CLI
   * - Build system
     - pnpm + tsdown (``dist/cli.js``)
   * - Ownership
     - Whiteboard-owned
   * - Upstream project
     - —
   * - Entrypoint
     - bins ``whiteboard`` and ``review`` → ``dist/cli.js``
   * - Inputs
     - argv, environment (``DEV_REVIEW_*``), discovery records
   * - Outputs
     - Human text, or NDJSON events with ``--json``; HTTP calls to a server
   * - Consumers
     - Users, agent skills (Pi), Git hooks, agent harness hooks
   * - Persistence
     - Reads ``$DEV_REVIEW_HOME``; writes ``default-instance`` and trace config
   * - Network dependency
     - Loopback HTTP to the server; GitHub for ``login`` and ``share``
   * - Credentials
     - Server token (read from discovery); GitHub login for share and hosted traces
   * - Verification
     - ``pnpm review version``, ``--help`` and ``cli.test.ts`` (run)
   * - Historical origin
     - Pre-import (``d1c6463c``); renamed with the package in #259
   * - Confidence
     - CONFIRMED

.. _reg-embedded-desktop-server:

Embedded Desktop server
~~~~~~~~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``packages/review/src/server/desktop-host.ts``, ``desktop-server.ts``
   * - Product feature
     - The single per-instance backend for Desktop, CLI and MCP
   * - Language
     - TypeScript
   * - Runtime
     - Node inside an Electron ``UtilityProcess``
   * - Build system
     - pnpm + tsdown (``dist/server/desktop-host.js``)
   * - Ownership
     - Whiteboard-owned
   * - Upstream project
     - —
   * - Entrypoint
     - ``runDesktopHost()``, started by ``reviewServerSupervisor.ts``
   * - Inputs
     - ``DEV_FAST_REVIEW_SERVER_*`` environment from Electron main; HTTP requests
   * - Outputs
     - A ``ready`` JSON line on stdout; HTTP/NDJSON/SSE responses; a discovery record
   * - Consumers
     - Desktop renderer, canvas, CLI, MCP adapter
   * - Persistence
     - ``review-api.db`` (SQLite), ``review-desktop/instances/<key>.json``
   * - Network dependency
     - Loopback listener; PostHog; spawns diffr; GitHub for PR reviews
   * - Credentials
     - Random 32-byte token from Electron main (``x-review-token``)
   * - Verification
     - ``desktop-server-*.test.ts`` (run); live launch NOT verified
   * - Historical origin
     - Pre-import; the JSON API arrived in #264
   * - Confidence
     - CONFIRMED

.. _reg-headless-server:

Headless server
~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``packages/review/src/server/headless-host.ts``
   * - Product feature
     - Authoring without Desktop (CI, GitHub Actions)
   * - Language
     - TypeScript
   * - Runtime
     - Node 24 (foreground process)
   * - Build system
     - pnpm + tsdown (part of ``dist/cli.js``)
   * - Ownership
     - Whiteboard-owned
   * - Upstream project
     - —
   * - Entrypoint
     - ``whiteboard server start`` → ``runHeadlessServer()``
   * - Inputs
     - ``--state-dir``, ``--port``; HTTP requests
   * - Outputs
     - HTTP API; a ``review-server`` discovery file
   * - Consumers
     - ``whiteboard api``, ``whiteboard mcp`` with ``--state-dir``
   * - Persistence
     - ``<state-dir>/review-api.db``, ``headless-server.lock``
   * - Network dependency
     - Loopback only (sharing publish optional)
   * - Credentials
     - Generated token in the discovery file
   * - Verification
     - Started, then drove create, edit, get and diff through ``api`` and MCP (run)
   * - Historical origin
     - #356 (2026-09-21)
   * - Confidence
     - CONFIRMED

.. _reg-json-review-store-and-http-api:

JSON review store and HTTP API
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``packages/review/src/review-api/``
   * - Product feature
     - Versioned, validated review documents pinned to commits
   * - Language
     - TypeScript
   * - Runtime
     - Node (inside either server)
   * - Build system
     - pnpm + tsdown (bundled)
   * - Ownership
     - Whiteboard-owned
   * - Upstream project
     - —
   * - Entrypoint
     - ``createReviewApi()`` in ``http.ts``, mounted at ``/reviews-api``
   * - Inputs
     - Commands (``POST /commands``), reads, uploads, activity leases
   * - Outputs
     - JSON snapshots, NDJSON watch streams, structural-diff NDJSON
   * - Consumers
     - Canvas, Desktop services, MCP and ``api`` adapters
   * - Persistence
     - SQLite tables: reviews, versions, receipts, repositories, resources, attention, authoring_sessions
   * - Network dependency
     - None beyond loopback; PR resolution uses ``gh`` or the GitHub API
   * - Credentials
     - Server token; ``gh`` credentials for private PRs
   * - Verification
     - ``review-api.test.ts`` and others (1185 tests run)
   * - Historical origin
     - #264–#270 (JSON Review 1/5–5/5)
   * - Confidence
     - CONFIRMED

.. _reg-mcp-adapter:

MCP adapter
~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``packages/review/src/review-api/mcp.ts``
   * - Product feature
     - Agent tool access
   * - Language
     - TypeScript
   * - Runtime
     - Node, a stdio child process of the agent
   * - Build system
     - pnpm + tsdown
   * - Ownership
     - Whiteboard-owned (uses the external ``@modelcontextprotocol/sdk``)
   * - Upstream project
     - MCP SDK 1.30.0
   * - Entrypoint
     - ``whiteboard mcp``
   * - Inputs
     - JSON-RPC over stdin (``initialize``, ``tools/list``, ``tools/call``)
   * - Outputs
     - JSON-RPC over stdout; text content, with ``isError`` on failure
   * - Consumers
     - Claude Code, Codex, Cursor, OpenCode
   * - Persistence
     - None (stateless per request)
   * - Network dependency
     - Loopback HTTP to the selected server
   * - Credentials
     - Server token from the discovery record
   * - Verification
     - Stdio smoke test: initialize, 28 tools, calls, error shape (run)
   * - Historical origin
     - #268; ``session_*`` names from #506
   * - Confidence
     - CONFIRMED

.. _reg-api-adapter:

``api`` adapter
~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``packages/review/src/review-api/agent-cli.ts``
   * - Product feature
     - Shell-based tool access (Pi skill, scripts, CI)
   * - Language
     - TypeScript
   * - Runtime
     - Node, one-shot
   * - Build system
     - pnpm + tsdown
   * - Ownership
     - Whiteboard-owned
   * - Upstream project
     - —
   * - Entrypoint
     - ``whiteboard api <tool> '<json>'``
   * - Inputs
     - Tool name plus a JSON argument (or ``-`` for stdin)
   * - Outputs
     - Tool result on stdout
   * - Consumers
     - Pi skill, GitHub Actions, humans
   * - Persistence
     - None
   * - Network dependency
     - Loopback HTTP
   * - Credentials
     - Server token from discovery
   * - Verification
     - ``api tools``, ``session_*`` calls (run)
   * - Historical origin
     - #268
   * - Confidence
     - CONFIRMED

.. _reg-canvas:

Canvas
~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``packages/review/app`` (``@dev.fast/review-canvas``)
   * - Product feature
     - Rendering documents, diagrams, traces, diffs; live drawing
   * - Language
     - TypeScript/TSX (React 19)
   * - Runtime
     - Chromium renderer inside the Desktop workbench (a dynamically imported ES module)
   * - Build system
     - pnpm + Vite 8 (``desktop.vite.config.ts``)
   * - Ownership
     - Whiteboard-owned
   * - Upstream project
     - Uses React, @xyflow/react, elkjs (EPL-2.0), libavoid-js (LGPL)
   * - Entrypoint
     - ``mountReviewCanvas()`` from the Vite bundle
   * - Inputs
     - Server URL and token; JSON snapshots; NDJSON watch lines
   * - Outputs
     - DOM; Copy for Agent clipboard text; UI telemetry through the host
   * - Consumers
     - ``reviewCanvasPart.ts`` in the fork
   * - Persistence
     - None of its own (per-viewer UI state only)
   * - Network dependency
     - Loopback HTTP to the server
   * - Credentials
     - Server token passed through the bridge
   * - Verification
     - 93 files and 517 tests, including Browser Mode (run with a Chromium shim)
   * - Historical origin
     - Pre-import as ``progressive-review/app``; split from the Node runtime in #366
   * - Confidence
     - CONFIRMED

.. _reg-libavoid-wasm-edge-router:

libavoid WASM edge router
~~~~~~~~~~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``node_modules/@mr_mint/elkjs-libavoid/dist/libavoid.wasm`` (copied by ``copy-canvas.mjs``)
   * - Product feature
     - Orthogonal edge routing in software maps and database lenses
   * - Language
     - C++ compiled to WebAssembly
   * - Runtime
     - WASM in the canvas's Chromium renderer
   * - Build system
     - Prebuilt npm artifact (not built here)
   * - Ownership
     - External (LGPL-2.1-or-later)
   * - Upstream project
     - ``Aksem/libavoid-js``
   * - Entrypoint
     - ``wasmUrl`` passed to the canvas session
   * - Inputs
     - Graph geometry from ELK
   * - Outputs
     - Routed edge paths
   * - Consumers
     - Software map and database lens views
   * - Persistence
     - —
   * - Network dependency
     - None
   * - Credentials
     - —
   * - Verification
     - ``database-lens.test.tsx`` loads it (run)
   * - Historical origin
     - Pre-import (documented in ``THIRD_PARTY_NOTICES.md``)
   * - Confidence
     - CONFIRMED

.. _reg-whiteboard-workbench--fork-owned-code:

Whiteboard workbench (fork-owned code)
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``apps/review-desktop/code-oss/src/vs/review/`` (149 files)
   * - Product feature
     - Home, review tabs, canvas host, diff views, install, update, telemetry
   * - Language
     - TypeScript
   * - Runtime
     - Electron main, renderer and utility processes
   * - Build system
     - Code - OSS gulp/esbuild (``build.sh``)
   * - Ownership
     - Whiteboard-owned inside a vendored tree
   * - Upstream project
     - Uses VS Code platform APIs
   * - Entrypoint
     - ``review.desktop.main.ts``; ``electron-main/*`` contributions
   * - Inputs
     - The server connection over IPC (``getConnection``), NDJSON streams
   * - Outputs
     - Workbench UI; starts the server process
   * - Consumers
     - End users
   * - Persistence
     - Code - OSS profile under ``review-desktop/state``
   * - Network dependency
     - Loopback; update feed
   * - Credentials
     - Generates the server token
   * - Verification
     - ``pnpm --filter @dev.fast/review-desktop test`` (197 tests run); build NOT verified
   * - Historical origin
     - Pre-import
   * - Confidence
     - CONFIRMED

.. _reg-vendored-code---oss:

Vendored Code - OSS
~~~~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``apps/review-desktop/code-oss/`` (6,406 tracked files)
   * - Product feature
     - Editor, LSP hosting, multi-diff, extension host, Electron app shell
   * - Language
     - TypeScript (+ upstream Rust ``cli/`` and ``build/win32``, both unbuilt here)
   * - Runtime
     - Electron 42.10.0
   * - Build system
     - npm (never pnpm) + gulp/esbuild; ``tsgo`` typecheck
   * - Ownership
     - Vendored upstream, with enumerated divergences
   * - Upstream project
     - ``microsoft/vscode`` at ``8a7abeba`` (code-oss-dev 1.129.1)
   * - Entrypoint
     - ``src/main.ts`` → ``vs/code/electron-main/app.ts``
   * - Inputs
     - —
   * - Outputs
     - The Desktop application
   * - Consumers
     - The Whiteboard workbench
   * - Persistence
     - User-data profile
   * - Network dependency
     - Build time: npm, Electron headers, GitHub; runtime: update feed
   * - Credentials
     - Apple signing and notarization for releases
   * - Verification
     - Dependency install and build FAILED here (network)
   * - Historical origin
     - ``d1c6463c`` import
   * - Confidence
     - CONFIRMED

.. _reg-diffr-semantic-diff-engine:

diffr semantic-diff engine
~~~~~~~~~~~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - Not in the repository. Fetched to ``packages/review/bin/diffr``
   * - Product feature
     - Semantic/structural diff, folding, hiding, summaries
   * - Language
     - Rust (derived from difftastic; tree-sitter 0.26, git2, wasmtime 34)
   * - Runtime
     - Native executable (macOS arm64, Linux x64 only)
   * - Build system
     - Cargo in ``devdotfast/diffr`` (external); fetched by ``diffr-fetch``
   * - Ownership
     - External (dev.fast-owned, separate repository)
   * - Upstream project
     - ``devdotfast/diffr``; difftastic (strings in the binary)
   * - Entrypoint
     - ``diffr --repo … --format ndjson --stream-annotations <base> <head> -- [paths]``
   * - Inputs
     - Repository path and revisions (reads Git objects itself)
   * - Outputs
     - NDJSON wire version 4: ``start``, ``file``, ``annotations``, ``complete``
   * - Consumers
     - ``server/structural-diff.ts``, ``diffr-config.ts``
   * - Persistence
     - User diffr config (``diffr config``)
   * - Network dependency
     - Only the optional summarize plugin (Gemini)
   * - Credentials
     - ``GEMINI_API_KEY`` / ``GOOGLE_API_KEY`` or a saved key, for summaries
   * - Verification
     - Fetched, run on a real commit, failure probes, ``test:integration:diffr`` 9/9 (run)
   * - Historical origin
     - Integrated in ``9af69be2``/#245; bundled in #394
   * - Confidence
     - CONFIRMED (binary behavior); UNVERIFIED (Rust source internals)

.. _reg-dev-fast-diffr-contract-package:

``@dev.fast/diffr`` contract package
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - npm dependency (``node_modules/@dev.fast/diffr``)
   * - Product feature
     - TypeScript mirror of diffr's wire format plus a binary fetcher
   * - Language
     - TypeScript + Node ESM script
   * - Runtime
     - Node (fetcher); anywhere zod runs (schemas)
   * - Build system
     - External (built with tsc and bun in ``devdotfast/diffr``)
   * - Ownership
     - External (dev.fast)
   * - Upstream project
     - ``devdotfast/diffr`` ``diffr-ts/``
   * - Entrypoint
     - ``decodeStructuralDiffEvent()``; bin ``diffr-fetch``
   * - Inputs
     - NDJSON lines; ``pins.json`` sha256 hashes
   * - Outputs
     - Validated ``StructuralDiffEvent``; a verified binary in ``bin/``
   * - Consumers
     - ``review-protocol`` (re-export), ``packages/review``
   * - Persistence
     - ``bin/diffr.stamp.json``
   * - Network dependency
     - GitHub releases (fetch only)
   * - Credentials
     - —
   * - Verification
     - Contract decode during the diffr run (run)
   * - Historical origin
     - Pinned at exactly 0.1.3 (#535)
   * - Confidence
     - CONFIRMED

.. _reg-review-protocol:

review-protocol
~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``packages/review-protocol``
   * - Product feature
     - Shared contracts: discovery, CLI install, bug report, code peeks, structural diff
   * - Language
     - TypeScript (zod 4)
   * - Runtime
     - Node, browser, and generated into the fork
   * - Build system
     - tsc; ``scripts/generate-native-source.mjs`` for the fork
   * - Ownership
     - Whiteboard-owned (private)
   * - Upstream project
     - Re-exports ``@dev.fast/diffr``
   * - Entrypoint
     - ``src/index.ts``
   * - Inputs
     - —
   * - Outputs
     - Schemas and types; ``code-oss/src/vs/review/common/reviewProtocol.ts`` (generated, gitignored)
   * - Consumers
     - Server, canvas, fork, CLI
   * - Persistence
     - —
   * - Network dependency
     - —
   * - Credentials
     - —
   * - Verification
     - 76 tests (run)
   * - Historical origin
     - Pre-import; ``json`` split out in #256; overlay generated at build since #98
   * - Confidence
     - CONFIRMED

.. _reg-sharing---review-share-protocol:

Sharing + review-share-protocol
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``packages/review/src/sharing``, ``packages/review-share-protocol``
   * - Product feature
     - Immutable review snapshots shared between machines
   * - Language
     - TypeScript
   * - Runtime
     - Node
   * - Build system
     - tsc (protocol, published 0.1.1); bundled (sharing)
   * - Ownership
     - Whiteboard-owned
   * - Upstream project
     - —
   * - Entrypoint
     - ``whiteboard share`` / ``share revoke``
   * - Inputs
     - A review version and its pinned GitHub checkout
   * - Outputs
     - Share link; imported shared review
   * - Consumers
     - Desktop ``contrib/sharing``, CLI
   * - Persistence
     - Local store; remote share host
   * - Network dependency
     - Yes: the hosted store ``https://app.dev.fast`` (``DEFAULT_STORE_ORIGIN``; overridable with ``DEV_REVIEW_SHARE_ORIGIN``)
   * - Credentials
     - GitHub login
   * - Verification
     - ``sharing/*.test.ts`` (run); remote service NOT verified
   * - Historical origin
     - #338
   * - Confidence
     - CONFIRMED (client); UNVERIFIED (service)

.. _reg-trace-core:

trace-core
~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``packages/trace-core``
   * - Product feature
     - Decision log: capture, sync, read and search agent sessions
   * - Language
     - TypeScript
   * - Runtime
     - Node (CLI and hooks), and inside the server
   * - Build system
     - pnpm + tsdown
   * - Ownership
     - Whiteboard-owned (private)
   * - Upstream project
     - —
   * - Entrypoint
     - ``whiteboard trace …``; hooks call ``whiteboard trace git-hook …``
   * - Inputs
     - Harness session files and hook events; Git commits
   * - Outputs
     - Uploaded sessions; ``Agent-Session`` trailers; normalized JSONL
   * - Consumers
     - CLI, server (Trace tab, quote validation), FFF search
   * - Persistence
     - ``$DEV_REVIEW_HOME/trace``, ``trace-search/``; Git notes ``refs/notes/dev-fast/*``
   * - Network dependency
     - S3/R2 (via the ``aws`` CLI) or the hosted store ``https://app.dev.fast``
   * - Credentials
     - AWS/R2 credentials, or a hosted login (Bearer token)
   * - Verification
     - 250 tests (run); real stores NOT verified
   * - Historical origin
     - Split out in #253–#256; consolidated into the CLI in #365
   * - Confidence
     - CONFIRMED

.. _reg-trace-protocol:

trace-protocol
~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``packages/trace-protocol``
   * - Product feature
     - Contract with the hosted trace store
   * - Language
     - TypeScript (zod)
   * - Runtime
     - Node (CLI) and the external store server
   * - Build system
     - tsc; published to npm (0.5.0)
   * - Ownership
     - Whiteboard-owned, published
   * - Upstream project
     - —
   * - Entrypoint
     - ``storeRoutes``, schemas
   * - Inputs
     - —
   * - Outputs
     - Schemas, limits, route builders and matchers
   * - Consumers
     - trace-core; the hosted store (external)
   * - Persistence
     - —
   * - Network dependency
     - —
   * - Credentials
     - —
   * - Verification
     - 17 tests (run)
   * - Historical origin
     - ``trace-shared`` (``9ae532cd``), renamed in #256
   * - Confidence
     - CONFIRMED

.. _reg-local-vcs:

local-vcs
~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``packages/local-vcs``
   * - Product feature
     - Pins, diffs, blob reads and file locks over Git and Jujutsu
   * - Language
     - TypeScript
   * - Runtime
     - Node; shells out to ``git`` and ``jj``
   * - Build system
     - tsdown; published (0.1.0)
   * - Ownership
     - Whiteboard-owned, published
   * - Upstream project
     - —
   * - Entrypoint
     - ``detectLocalVcs()``, ``git()``, ``resolveRevision()``
   * - Inputs
     - Repository paths and revisions
   * - Outputs
     - Resolved commits, diffs, file contents
   * - Consumers
     - review-api, trace-core, sharing
   * - Persistence
     - Git notes, lock files
   * - Network dependency
     - ``git fetch`` of PR refs (through review-api)
   * - Credentials
     - The user's Git credentials
   * - Verification
     - 69 tests (run)
   * - Historical origin
     - Pre-import
   * - Confidence
     - CONFIRMED

.. _reg-json:

json
~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``packages/json``
   * - Product feature
     - JSON decoding at boundaries
   * - Language
     - TypeScript
   * - Runtime
     - Node / browser
   * - Build system
     - tsc
   * - Ownership
     - Whiteboard-owned (private)
   * - Upstream project
     - —
   * - Entrypoint
     - ``jsonObject``, ``jsonString``, ``parseJsonText``
   * - Inputs
     - Untyped JSON
   * - Outputs
     - Typed values
   * - Consumers
     - Most packages
   * - Persistence
     - —
   * - Network dependency
     - —
   * - Credentials
     - —
   * - Verification
     - Typecheck (run)
   * - Historical origin
     - Split from review-protocol in #256
   * - Confidence
     - CONFIRMED

.. _reg-claude---codex---cursor-plugins:

Claude / Codex / Cursor plugins
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``packages/agent-plugins/{claude,codex,cursor}``, ``.claude-plugin/``, ``.agents/plugins/``
   * - Product feature
     - Connect coding agents
   * - Language
     - JSON manifests
   * - Runtime
     - The agent host
   * - Build system
     - None
   * - Ownership
     - Whiteboard-owned
   * - Upstream project
     - Agent plugin formats (external)
   * - Entrypoint
     - ``.mcp.json``: ``sh -c 'exec "$HOME/.local/bin/whiteboard" mcp'``
   * - Inputs
     - —
   * - Outputs
     - An MCP server registration
   * - Consumers
     - Claude Code, Codex, Cursor
   * - Persistence
     - —
   * - Network dependency
     - —
   * - Credentials
     - —
   * - Verification
     - ``agent-plugins.test.ts`` (run); real hosts NOT verified
   * - Historical origin
     - #475
   * - Confidence
     - CONFIRMED

.. _reg-opencode-plugin:

OpenCode plugin
~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``packages/agent-plugins/opencode``
   * - Product feature
     - Connect OpenCode
   * - Language
     - JavaScript
   * - Runtime
     - OpenCode's plugin runtime
   * - Build system
     - None (published as-is, 0.1.1)
   * - Ownership
     - Whiteboard-owned
   * - Upstream project
     - ``@opencode-ai/plugin`` (types)
   * - Entrypoint
     - Default export ``whiteboardPlugin()`` → ``config`` hook
   * - Inputs
     - OpenCode config
   * - Outputs
     - ``config.mcp.whiteboard`` (local command)
   * - Consumers
     - OpenCode
   * - Persistence
     - —
   * - Network dependency
     - —
   * - Credentials
     - —
   * - Verification
     - Test coverage in ``agent-plugins.test.ts`` (run)
   * - Historical origin
     - #84, then #465, then #475
   * - Confidence
     - CONFIRMED

.. _reg-pi-skill-package:

Pi skill package
~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``packages/agent-plugins/pi``
   * - Product feature
     - Connect Pi (skill, not MCP)
   * - Language
     - Markdown skill
   * - Runtime
     - Pi agent
   * - Build system
     - None (published, 0.1.1)
   * - Ownership
     - Whiteboard-owned
   * - Upstream project
     - Pi package conventions
   * - Entrypoint
     - ``skills/whiteboard/SKILL.md``
   * - Inputs
     - —
   * - Outputs
     - Instructs Pi to run ``whiteboard api …``
   * - Consumers
     - Pi
   * - Persistence
     - —
   * - Network dependency
     - —
   * - Credentials
     - —
   * - Verification
     - Test coverage (run)
   * - Historical origin
     - #475, #537
   * - Confidence
     - CONFIRMED

.. _reg-agent-instructions:

Agent instructions
~~~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``packages/review/instructions/*.md``
   * - Product feature
     - Server-served guidance (authoring, lenses, scratchpad, trace archaeology)
   * - Language
     - Markdown
   * - Runtime
     - Served by the server
   * - Build system
     - Shipped in the npm package
   * - Ownership
     - Whiteboard-owned
   * - Upstream project
     - —
   * - Entrypoint
     - ``session_get_instructions({topic})``
   * - Inputs
     - Topic; capability context
   * - Outputs
     - Text
   * - Consumers
     - Agents
   * - Persistence
     - —
   * - Network dependency
     - —
   * - Credentials
     - —
   * - Verification
     - ``instructions.test.ts`` (run)
   * - Historical origin
     - The ``skills/dev-review`` skill pre-import; rewritten in #463
   * - Confidence
     - CONFIRMED

.. _reg-legacy-importer:

Legacy importer
~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``packages/review/src/review-import/``, ``migrate.ts``
   * - Product feature
     - Migrate MDX reviews to JSON
   * - Language
     - TypeScript
   * - Runtime
     - Node (startup, ``migrate apply``)
   * - Build system
     - Bundled
   * - Ownership
     - Whiteboard-owned (legacy support)
   * - Upstream project
     - —
   * - Entrypoint
     - Startup importer; ``whiteboard migrate apply``
   * - Inputs
     - ``$DEV_REVIEW_HOME/reviews/<uuid>/``
   * - Outputs
     - JSON reviews
   * - Consumers
     - Server startup
   * - Persistence
     - Reads the legacy tree; writes the store
   * - Network dependency
     - —
   * - Credentials
     - —
   * - Verification
     - Golden fixtures (run; needs Git ``safe.directory`` under root)
   * - Historical origin
     - #296; the MDX writer was removed in #312 and #322
   * - Confidence
     - CONFIRMED

.. _reg-telemetry:

Telemetry
~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``review-telemetry.ts``, ``ui-telemetry-events.ts``, ``posthog-capture-client.ts``
   * - Product feature
     - Anonymous usage and reliability data
   * - Language
     - TypeScript
   * - Runtime
     - Node (servers and CLI); events relayed from the renderer
   * - Build system
     - Bundled; a key is embedded at release (``embed-posthog-key.mjs``)
   * - Ownership
     - Whiteboard-owned; the PostHog service is external
   * - Upstream project
     - PostHog
   * - Entrypoint
     - ``ReviewTelemetry.fromEnv()``
   * - Inputs
     - Events; opt-out environment variables
   * - Outputs
     - HTTPS to ``https://us.i.posthog.com``
   * - Consumers
     - Product team
   * - Persistence
     - Install ID; queued events
   * - Network dependency
     - Yes
   * - Credentials
     - Embedded project key
   * - Verification
     - ``test:telemetry-privacy`` tests (run); delivery NOT verified
   * - Historical origin
     - Pre-import; contract formalized in #533 and #550
   * - Confidence
     - CONFIRMED

.. _reg-curated-extensions-and-language-servers:

Curated extensions and language servers
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``apps/review-desktop/scripts/curated-extensions*.mjs``
   * - Product feature
     - LSP navigation (Rust, Swift, C#, Python …) and keymaps
   * - Language
     - Various (third-party VSIX)
   * - Runtime
     - Code - OSS extension host
   * - Build system
     - Downloaded from ``https://open-vsx.org`` at build time
   * - Ownership
     - External
   * - Upstream project
     - Open VSX publishers
   * - Entrypoint
     - ``curated-extensions.mjs``
   * - Inputs
     - Manifest
   * - Outputs
     - Extensions under ``code-oss/extensions`` (gitignored)
   * - Consumers
     - Desktop
   * - Persistence
     - —
   * - Network dependency
     - Yes (open-vsx.org)
   * - Credentials
     - —
   * - Verification
     - NOT verified (open-vsx.org blocked here)
   * - Historical origin
     - Pre-import
   * - Confidence
     - CONFIRMED

.. _reg-desktop-build-and-packaging-scripts:

Desktop build and packaging scripts
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``apps/review-desktop/scripts/*.sh``, ``*.mjs``, ``*.py``
   * - Product feature
     - Build, run, package (DMG, Squirrel zips, Fedora RPM repositories)
   * - Language
     - Bash, Node ESM, Python
   * - Runtime
     - Developer machine / CI runner
   * - Build system
     - —
   * - Ownership
     - Whiteboard-owned
   * - Upstream project
     - Wraps the Code - OSS build
   * - Entrypoint
     - ``build.sh``, ``run.sh``, ``package-macos.sh``, ``package-linux*.sh``
   * - Inputs
     - The repository
   * - Outputs
     - ``.build/electron``, packaged apps
   * - Consumers
     - ``pnpm dev``, release workflows
   * - Persistence
     - Freshness stamps in ``code-oss/.build/dev-fast``
   * - Network dependency
     - Yes
   * - Credentials
     - Apple signing (releases)
   * - Verification
     - Script unit tests (105 run); the build itself FAILED (network)
   * - Historical origin
     - Pre-import
   * - Confidence
     - CONFIRMED

.. _reg-cli-release-and-smoke-scripts:

CLI release and smoke scripts
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``scripts/*.mjs``
   * - Product feature
     - Pack, smoke-test and publish ``@dev.fast/review``
   * - Language
     - Node ESM
   * - Runtime
     - CI
   * - Build system
     - —
   * - Ownership
     - Whiteboard-owned
   * - Upstream project
     - —
   * - Entrypoint
     - ``review-cli-release.mjs``, ``pack-review-cli.mjs``, ``smoke-review-cli.mjs``
   * - Inputs
     - Workspace
   * - Outputs
     - Tarball; npm publication
   * - Consumers
     - ``review-cli-release.yml``
   * - Persistence
     - —
   * - Network dependency
     - npm registry
   * - Credentials
     - npm provenance (OIDC)
   * - Verification
     - ``node --test scripts/**/*.test.mjs`` 11/11 (run)
   * - Historical origin
     - #367
   * - Confidence
     - CONFIRMED

.. _reg-github-actions-workflows:

GitHub Actions workflows
~~~~~~~~~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``.github/workflows/*.yml``
   * - Product feature
     - CI, desktop preview and release, CLI release, Linux packages
   * - Language
     - YAML
   * - Runtime
     - ``ubuntu-latest``, ``review_big_boy``, ``macos-15-xlarge``
   * - Build system
     - —
   * - Ownership
     - Whiteboard-owned
   * - Upstream project
     - Pinned third-party actions (SHA-pinned, #19)
   * - Entrypoint
     - Push, PR and ``workflow_dispatch`` triggers
   * - Inputs
     - Repository
   * - Outputs
     - Checks, releases
   * - Consumers
     - Maintainers
   * - Persistence
     - —
   * - Network dependency
     - Yes
   * - Credentials
     - Apple, npm and GitHub secrets
   * - Verification
     - NOT run in this fork (no check runs on PR #1)
   * - Historical origin
     - Pre-import
   * - Confidence
     - CONFIRMED

.. _reg-anti-slop-lint-plugin:

anti-slop lint plugin
~~~~~~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``tools/oxlint/anti-slop``
   * - Product feature
     - Code quality rules
   * - Language
     - TypeScript (oxlint JS plugin)
   * - Runtime
     - oxlint
   * - Build system
     - Loaded from source by oxlint
   * - Ownership
     - Vendored (partially) + Whiteboard-owned rules
   * - Upstream project
     - ``dmmulroy/anti-slop``
   * - Entrypoint
     - ``.oxlintrc.json`` ``jsPlugins``
   * - Inputs
     - Source
   * - Outputs
     - Diagnostics
   * - Consumers
     - ``pnpm lint``
   * - Persistence
     - —
   * - Network dependency
     - —
   * - Credentials
     - —
   * - Verification
     - ``pnpm lint`` (run)
   * - Historical origin
     - #95
   * - Confidence
     - CONFIRMED

.. _reg-review-latency-harness:

review-latency harness
~~~~~~~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``scripts/review-latency``
   * - Product feature
     - Measure prompt-to-document latency for authoring runs
   * - Language
     - Python ≥3.12
   * - Runtime
     - CPython via ``uv``
   * - Build system
     - hatchling (``pyproject.toml``), ``uv.lock``
   * - Ownership
     - Whiteboard-owned (developer tool)
   * - Upstream project
     - —
   * - Entrypoint
     - ``uv run review-latency``
   * - Inputs
     - ``runs.toml``; an installed ``/Applications/Whiteboard.app``; ``claude``
   * - Outputs
     - Run directories, HTML dashboards
   * - Consumers
     - Developers
   * - Persistence
     - ``~/.dev/review-latency/runs``
   * - Network dependency
     - Agent API
   * - Credentials
     - Agent login
   * - Verification
     - ``uv run review-latency --help`` (run); real runs need macOS
   * - Historical origin
     - #157
   * - Confidence
     - CONFIRMED

.. _reg-sphinx-documentation:

Sphinx documentation
~~~~~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``docs/``
   * - Product feature
     - Developer documentation
   * - Language
     - reStructuredText + Python config
   * - Runtime
     - Python / Sphinx 9
   * - Build system
     - ``sphinx-build``
   * - Ownership
     - Whiteboard-owned
   * - Upstream project
     - Sphinx, sphinxcontrib-mermaid
   * - Entrypoint
     - ``docs/conf.py``
   * - Inputs
     - ``.rst`` files
   * - Outputs
     - HTML
   * - Consumers
     - Developers
   * - Persistence
     - ``docs/_build`` (gitignored)
   * - Network dependency
     - Mermaid from a CDN at view time
   * - Credentials
     - —
   * - Verification
     - ``sphinx-build -W`` + Mermaid render check (run)
   * - Historical origin
     - PR #1 in this fork
   * - Confidence
     - CONFIRMED

.. _reg-progressive-review-remnant:

progressive-review remnant
~~~~~~~~~~~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``packages/progressive-review/tutorial/data.ts``
   * - Product feature
     - None today (legacy tutorial data)
   * - Language
     - TypeScript
   * - Runtime
     - —
   * - Build system
     - Not a workspace package (no ``package.json``)
   * - Ownership
     - Legacy
   * - Upstream project
     - —
   * - Entrypoint
     - —
   * - Inputs
     - —
   * - Outputs
     - —
   * - Consumers
     - UNVERIFIED (no importer found by name)
   * - Persistence
     - —
   * - Network dependency
     - —
   * - Credentials
     - —
   * - Verification
     - —
   * - Historical origin
     - The main package until #259 renamed ``progressive-review`` to ``review``
   * - Confidence
     - HISTORY

.. _reg-upstream-rust-in-code---oss:

Upstream Rust in Code - OSS
~~~~~~~~~~~~~~~~~~~~~~~~~~~

.. list-table::
   :widths: 22 78

   * - Repository path
     - ``code-oss/cli/`` (VS Code CLI), ``code-oss/build/win32/`` (Inno updater)
   * - Product feature
     - None in Whiteboard (not built or shipped)
   * - Language
     - Rust
   * - Runtime
     - —
   * - Build system
     - Cargo (upstream), never invoked by Whiteboard scripts
   * - Ownership
     - Vendored upstream
   * - Upstream project
     - ``microsoft/vscode``
   * - Entrypoint
     - —
   * - Inputs
     - —
   * - Outputs
     - —
   * - Consumers
     - None found
   * - Persistence
     - —
   * - Network dependency
     - —
   * - Credentials
     - —
   * - Verification
     - ``git grep`` finds no build reference in scripts or workflows
   * - Historical origin
     - ``d1c6463c`` import
   * - Confidence
     - INFERRED
